

// user item for storing the user item in the database
export type UserItem={
    id?: string,
    name: string,
    age: number,
    email: string,
    phone: string,
    password: string,
    usertype:string
}